# Senha pra equipe salvar mudanças (compartilhada via canal seguro)
EDIT_PASSWORD=trocar-essa-senha

# Caminho do arquivo de estado persistente
# Local: ./data/state.json
# Railway: /data/state.json (volume montado em /data)
DATA_FILE=./data/state.json

# Porta do servidor (Railway define automaticamente)
PORT=3000
